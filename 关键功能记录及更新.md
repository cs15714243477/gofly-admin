极速房经济带看系统

小程序端

只针对经纪人带看房使用

查看系统公布房源

可以分享给客户查看房源信息

可以根据房源去带看进行蓝牙开锁和远程密码开锁

可以设置我关注的房源

支持微信分享，分享经纪人卡片

支持对房源的分享，带经纪人的个人信息

banner第一页加入视频

经纪人根据字段是否可以更新房源信息

E:\Work\Progect\GoFly-admin\main-admin\web\wxapp小程序代码

E:\Work\Progect\GoFly-admin\main-admin\app\business\uniapp小程序接口

后端

可以设置管理员，可以设置对应的查看权限

对经纪人进行管理

对房源进行管理

房主信息，电话， 收房人信息，收房人电话，收房价格，（给业主支付多少钱），修改状态要做记录

房源手机端录入

房源图片和视频，可以单独，下载，是否允许下载

房源推荐房源

0首付房源

分享记录

维护人还需要加上，可以显示后台，还是经纪人

经纪人表单还少东西    

增加按钮和保存按钮不好看

新增经纪人可以维护智能锁

手机编辑装修模块放在最后,地理位置没有省市区选择 因为address 里面省市区和详细地址是合在一起的，装修阶段下拉字段也是后台配置获取的

房源增加维护人信息，为0是后台管理员添加

正在添加智能锁管理界面

**登录页面 去掉手机号验证码登录

pages/agent_public_card/agent_public_card，分享页面可以设置下面显示的房源，用户从此点入的房源详情只能看到基础信息，看不到佣金，用户也不用登录，也不能去别的页面，里面可以展示系统推荐和经纪人推荐，经纪人可以自己设置


**装修说明框占满**

**注册的时候 姓名随机  微信号默认值   **

**保存海报权限设置**

**房源分享相关逻辑**

**关于我们和推荐给朋友**

**

房源分享 连接不对

后台首页和登录页

# 

# 

## 2026-02-13（需求整理/优化版）

> 来源：`功能介绍.md`（行 87-113）原始记录。以下为落地化整理版（含范围、规则、验收与 Prompt）。

### 一、范围（已确认）
- 业务端后台：`web/business`
- 微信小程序：`web/wxapp`
- 后端 API：`app/business/uniapp`、`app/business/houses`
- 数据库：MySQL（`schema.sql`）

### 二、全局规则与约束（已确认）
1. 经纪人登录：必须后台审核通过才允许登录（登录接口拦截，未通过不发 token）。
2. 房源可见性：小程序端对 `已售(sold)` / `下架(off_market)` 房源 **完全不可见**；业务端后台可查看。
3. 浏览记录：每次进入房源详情都写入 1 条浏览记录（不去重）。
4. 数字链接：房源详情页“浏览/带看”数字可点击，跳转到个人中心对应记录页，并默认按 `property_id` 过滤。
5. 佣金展示：删除“签约后7个工作日内结算”；UI 改为“房源标签下面的一条紧凑单行展示”（高度不要太高）。
6. 预售房源可见性：小程序端 `sale_status=in_sale` 仅在“预售 Tab”可见，其他入口不展示；详情仅允许从预售 Tab 进入（需 view_key 校验）。

### 三、需求清单（建议优先级）
| 优先级 | 功能点 | 端（B/C/D/E） | 当前状态 | 核心验收要点 |
|---|---|---|---|---|
| P0 | 经纪人审核后才能登录 | C + D + E | 进行中 | 未审核不发 token；审核中/拒绝有明确提示与原因 |
| P0 | 业务端后台：房源观看记录（真实数据） | B + D + E | 待做 | 可看到“哪个经纪人、看了哪个房源、何时、次数/明细” |
| P0 | 业务端后台：房源开锁记录 | B + D + E | 进行中 | 可按房源查看开锁申请/结果/方式/时间/经纪人 |
| P0 | 通通锁：云端已删锁，本地也删除 | B + D + E | 待做 | 同步后本地锁表清理 + 解绑房源 + `has_smart_lock=0` |
| P1 | 装修：全进度/流程状态可见 | C + D + E | 待做 | 展示工序时间线与每步状态（如：完成） |
| P1 | 房源详情：自定义描述模块 | C + D + E | 进行中 | 字段可保存/展示；为空有占位提示 |
| P1 | 房源详情：浏览/带看数字可跳转到个人中心记录（按房源过滤） | C + D | 待做 | 跳转后默认只显示当前房源记录 |
| P1 | 经纪人跟进记录：登记需绑定对应房源 | C + D + E | 待补充 | 跟进记录与房源 `property_id` 强关联，可回溯 |
| P1 | 已售/下架：经纪人端不可见，后台可见 | B + C + D | 已确认规则 | 小程序侧全链路不可见（含记录跳转） |
| P1 | 房源详情信息优化 | C | 待补充 | 明确要优化的模块/顺序/字段/文案 |
| P1 | 预售房源可单独看见 | B + C + D + E | 已完成 | `sale_status=in_sale` 仅在“预售 Tab”展示；详情需 view_key |
| P1 | 智能锁同步：显示别名 `lockAlias` + 电量 | B + D + E | 待做 | 锁列表/详情可见别名与电量，数据与云端一致 |

### 四、详细需求说明（落地版）
#### 4.1 装修进度：全流程可视化（P1）
- 目标：房源详情可看到装修“当前状态 + 施工时间线 + 每个工序状态（待做/进行中/完成）+ 图片（可选）”。
- 数据来源建议：
  - `business_renovations`：`renovation_status`、`progress_percentage`、`current_stage`、`stage_logs(JSON)` 等。
- 展示要求：
  - 至少支持状态文案示例：完成。
  - 有时间线时按工序展示；无数据时给出“暂无装修/暂无进度”的友好提示。
- 验收：
  - 同一房源在不同状态下（none/in_progress/done）展示正确；
  - 时间线状态与图片可正常预览。

#### 4.2 经纪人审核后才能登录（P0）
- 目标：经纪人资料提交后进入审核；审核未通过前不可登录小程序。
- 后端行为（接口拦截）：
  - 登录接口直接返回“审核中/已拒绝”等状态，不返回 token。
  - 已拒绝需携带拒绝原因（如有）。
- 业务端后台（B 端）：
  - 提供经纪人审核入口：列表（待审/通过/拒绝）、详情、通过/拒绝（可填原因）。
- 验收：
  - 未注册/未提交资料：提示去完善资料；
  - 审核中：提示等待；
  - 审核拒绝：提示原因并可重新提交；
  - 审核通过：可正常登录并进入系统。

#### 4.3 业务端后台：房源观看记录（真实数据）（P0）
- 目标：后台可按房源查看“哪个经纪人看了哪个房源”，数据真实可追溯。
- 数据口径（已确认）：每次进入房源详情写 1 条浏览记录（不去重）。
- 展示维度建议：
  - 经纪人（姓名/手机号/门店可选）
  - 房源（标题/小区）
  - 时间（最新、分页）
  - 可选：来源页面/渠道（来自 `meta_data`）
- 验收：
  - 有实际浏览行为时后台能查到对应记录；
  - 记录可按时间倒序，分页正常。

#### 4.4 业务端后台：房源开锁记录（P0）
- 目标：后台可按房源查看开锁申请与结果。
- 展示维度建议：
  - 开锁方式：蓝牙/密码
  - 申请人（经纪人）
  - 状态：pending/approved/rejected/completed/cancelled
  - 时间：申请/完成
  - 备注：失败原因/审批备注
- 验收：
  - 有开锁行为时可追溯到房源与经纪人；
  - 状态与时间展示正确。

#### 4.5 房源详情：自定义描述模块（P1）
- 目标：房源详情可展示一段可编辑的自定义描述。
- 字段建议：`business_properties.custom_desc`
- 交互：
  - 无内容显示“暂无描述”；
  - 有内容完整展示（长文可折叠/滚动，按实际 UI 规范）。
- 验收：新增/编辑/查看一致，内容保存不丢失。

#### 4.6 数字链接：浏览/带看跳转个人中心记录（P1）
- 目标：房源详情页的“浏览/带看”数字可点击。
- 跳转规则（已确认）：跳到个人中心对应记录页，并默认按当前 `property_id` 过滤。
- 验收：点击后进入正确页面，列表默认只显示当前房源记录。

#### 4.7 已售/下架可见性（P1，规则已确认）
- 小程序端：对 `sold/off_market` 房源完全不可见（含列表、搜索、推荐、记录跳转进入详情）。
- 业务端后台：可见且可查询相关记录（用于管理追溯）。

#### 4.8 预售房源可单独查看（P1）
- 状态码（已确认建议）：`sale_status = in_sale`
- 要求：
  - 小程序端提供独立筛选/Tab（示例：预售）。
  - 业务端后台同样支持筛选/列表查看。
- 验收：预售房源能被单独筛选出来，且状态展示正确。

#### 4.9 智能锁同步展示：`lockAlias` + 电量（P1）
- 目标：锁列表/锁详情展示云端别名 `lockAlias` 与电量。
- 备注：当前锁主数据表为 `business_smart_locks`，可先从 `raw_json` 解析展示别名，或新增字段落库（视后续查询性能而定）。
- 验收：同步后展示的别名与电量与云端一致。

#### 4.10 通通锁删除联动（P0）
- 目标：云端已删除的锁，本地同步后也要删除，避免“本地还有但云端没了”的脏数据。
- 行为（已确认方案 A）：
  - 本地锁表软删除；
  - 自动解绑相关房源；
  - 房源 `has_smart_lock=0`。
- 验收：云端删锁→后台同步→本地不再显示该锁且房源不再显示绑定状态。

#### 4.11 经纪人跟进记录绑定房源（P1，待补充）
- 需要你补充：跟进记录当前在哪个页面/表？字段是否已包含 `property_id`？
- 目标：每条跟进记录都能明确指向一个房源，便于在房源维度汇总跟进。

### 五、待补充/未决问题
- “房源详情加入信息优化”：请列出要优化的具体模块（例如：字段顺序、展示组件、文案、隐藏/显示规则）。
- “经纪人跟进记录”现有实现位置/表结构（便于补齐落地方案）。

### 六、AI Prompt（可直接复制使用）
```text
你是资深产品经理 + 全栈技术负责人。
请根据《关键功能记录及更新.md》里“2026-02-13（需求整理/优化版）”这一节，输出以下内容：
1) PRD（目标、范围、用户角色、流程、边界条件、验收标准）
2) 数据库变更建议（表/字段/索引/迁移策略；若不需要也说明原因）
3) API 清单（路径、方法、入参、返回、错误码、权限校验、分页规则）
4) 前端任务拆分（web/business + web/wxapp），每项注明页面/组件/交互点
5) 后端任务拆分（app/business/uniapp + app/business/houses），每项注明影响表与关键逻辑
6) 测试用例（功能/权限/异常/回归）

输出要求：
- 全中文
- 结构化分级标题
- 每个需求点都给“验收标准（可测）”
```
